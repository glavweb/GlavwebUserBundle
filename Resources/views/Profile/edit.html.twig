{% extends base_template %}

{% trans_default_domain 'FOSUserBundle' %}

{% block content %}
    {{ form_start(form, {
        'method' : 'POST',
        'action' : path('fos_user_profile_edit'),
        'style': 'horizontal',
        'label_col' : 2,
        'widget_col' : 10,
    }) }}
        {{ form_widget(form) }}
        <div>
            <input type="submit" value="{{ 'profile.edit.submit'|trans }}" class="btn btn-success pull-right" />
        </div>
    {{ form_end(form) }}
{% endblock %}


